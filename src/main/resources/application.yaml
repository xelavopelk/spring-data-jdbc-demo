
server:
  port: 8189
  servlet:
    context-path: /app
spring:
  datasource:
    url: jdbc:postgresql://localhost:15430/otus
    password: otus
    username: otus
    #driver-class-name: org.h2.Driver
    #url: jdbc:h2:mem:appdb;MODE=PostgreSQL
    #username: sa
    #password:
  #h2:
   # console:
     # enabled: true
logging:
  level:
    org.springframework.jdbc.core.JdbcTemplate: TRACE